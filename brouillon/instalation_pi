sudo apt update && sudo apt install -y python3-pip python3-venv git
python3 -m venv .venv && source .venv/bin/activate
pip install flask gpiozero
# Choisis ton ADC:
pip install adafruit-circuitpython-ads1x15   # si ADS1115 (I2C)
# OU
pip install adafruit-circuitpython-mcp3xxx   # si MCP3008 (SPI)

mkdir smartlight && cd smartlight
# → colle ici les 4 fichiers dans l'arborescence indiquée
python app.py
